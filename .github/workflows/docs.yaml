name: Build and Deploy
on:
  push:
    branches:
      - docs

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@docs

    - name: Build and Deploy
      uses: JacksonMaxfield/github-pages-deploy-action-python@docs
      env:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        BASE_BRANCH: docs # The branch the action should deploy from.
        BRANCH: gh-pages # The branch the action should deploy to.
        FOLDER: docs/_build/html # The folder the action should deploy. This example folder is generated by Sphinx
        BUILD_SCRIPT: pip install .[all] && cd docs && make html && touch docs/_build/html/.nojekyll # The build script the action should run prior to deploying.
